declare type Options = { // eslint-disable-line no-undef
    attachToDocument?: boolean,
    mocks?: Object,
    slots?: Object,
    localVue?: Component,
    provide?: Object,
    stubs?: Object,
    context?: Object,
    attrs?: Object,
    listeners?: Object
}
