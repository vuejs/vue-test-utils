declare type Options = { // eslint-disable-line no-undef
    attachToDocument?: boolean,
    mocks?: Object,
    slots?: Object,
    localVue?: Component,
    stubs?: Object,
    context?: Object,
    clone?: boolean,
    attrs?: Object,
    listeners?: Object
}
