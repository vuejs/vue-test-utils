declare type Options = { // eslint-disable-line no-undef
    attachToDocument?: boolean,
    mocks?: Object,
    methods?: Object,
    slots?: Object,
    scopedSlots?: Object,
    localVue?: Component,
    provide?: Object,
    stubs?: Object,
    context?: Object,
    attrs?: Object,
    listeners?: Object
}
