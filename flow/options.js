declare type Options = { // eslint-disable-line no-undef
    attachToDocument?: boolean,
    intercept?: Object,
    slots?: Object,
    localVue?: Component,
    stubs?: Object,
    context?: Object,
    clone?: boolean
}
